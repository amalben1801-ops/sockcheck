services:
  - type: web
    name: sockcheck
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SOCKCHECK_SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.12.0
